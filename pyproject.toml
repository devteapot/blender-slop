[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "blender-slop"
version = "0.1.0"
description = "A native Blender add-on that exposes Blender as a SLOP provider."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [
  { name = "Codex" }
]
keywords = ["blender", "slop", "ai", "3d", "provider"]
dependencies = [
  "slop-ai[websocket]>=0.2.0,<0.3.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]

[tool.hatch.build.targets.wheel]
packages = ["addon/blender_slop_addon"]

[tool.hatch.build.targets.sdist]
include = [
  "tests",
  "addon",
  "README.md",
  "LICENSE",
  "NOTICE",
  "pyproject.toml",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
